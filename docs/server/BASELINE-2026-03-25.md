# Buddy Server 基线说明（2026-03-25）

## 1. 文档目的

本文件记录 `buddy-server` 在 2026-03-25 建立的“可运行基线”状态，作为后续开发、联调、回滚和问题排查的参考点。

这份基线用于：

- 标记第一个本地可运行的后端版本
- 明确当前已经跑通的能力
- 固定当前可用的环境配置
- 为后续功能开发提供回退锚点
- 让团队在环境或功能异常时有统一参考版本

## 2. 当前基线状态

当前版本已经完成以下关键目标。

### 2.1 服务启动

- 后端服务已可在本地正常启动。
- 启动命令：`pnpm dev`
- 服务地址：`http://localhost:3000`

### 2.2 数据链路

- PostgreSQL 已打通。
- Redis 已打通，本地端口为 `6380`。
- Prisma Client 已生成。
- Prisma migration 已执行成功。
- 数据库与 schema 已同步。

### 2.3 Git 基线

- 本地仓库已完成整理。
- GitHub 仓库已创建并推送成功。
- `main` 分支已建立。
- `develop` 分支已建立。
- 当前基线已具备继续协作开发条件。

## 3. 已验证通过的接口

以下接口已完成人工验证并确认可用。

### 3.1 健康检查

- `GET /`
- 验证结果：服务可用，能够返回正常响应。

### 3.2 认证模块

- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`

验证结果：

- 用户可正常注册。
- 用户可正常登录。
- 登录后可获取 token。

### 3.3 宠物模块

- `POST /api/v1/pets`
- `GET /api/v1/pets/:petId`

验证结果：

- 可成功创建宠物。
- 可成功读取宠物信息。

## 4. 当前推荐开发环境

### 4.1 操作系统

- Windows 11 23H2
- WSL2 Ubuntu 24.04

### 4.2 项目路径

WSL 路径：

```text
/home/openclaw/.openclaw/workspace/buddy-server
```

Windows 映射路径：

```text
\\wsl.localhost\Ubuntu-24.04\home\openclaw\.openclaw\workspace\buddy-server
```

### 4.3 运行依赖

- Node.js
- pnpm
- Bun
- PostgreSQL
- Redis
- Prisma

## 5. 当前固定环境配置

本基线统一采用以下本地开发配置：

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/buddy_dev?schema=public"
REDIS_URL="redis://127.0.0.1:6380"
PORT=3000
NODE_ENV=development
```

说明：

- PostgreSQL 用户名：`postgres`
- PostgreSQL 密码：`postgres`
- 数据库名：`buddy_dev`
- Redis 端口：`6380`
- 服务端口：`3000`

## 6. 本基线中已修复的关键问题

### 6.1 环境与依赖问题

- 修复 WSL 代理导致的 npm / pnpm 安装阻塞。
- 在本地安装并启用 pnpm。
- 安装 Bun 并恢复 `pnpm dev` 启动链路。
- 打通 Git 通过本地代理访问 GitHub 的能力。

### 6.2 数据与迁移问题

- PostgreSQL 本地连接打通。
- Redis 改为本地手动实例，端口为 `6380`。
- Prisma Client 生成成功。
- Prisma migration 成功应用到 `buddy_dev`。

### 6.3 代码问题

- Token 字段从 `sub` 统一修复为 `userId`。
- Prisma 关联写法修复。
- 路由导出结构修复。
- 宠物相关 schema 字段映射修复。
- `PetStage` 枚举修复。
- 服务启动链路修复完成。

## 7. 当前 Git 状态

### 7.1 远端仓库

```text
https://github.com/shiwudu111/buddy-server.git
```

### 7.2 分支说明

- `main`：稳定主分支，保存当前可运行基线。
- `develop`：日常开发集成分支。

### 7.3 当前要求

后续开发默认在 `develop` 分支进行。仅在明确验证通过后，才合并到 `main`。

## 8. 当前仍未完成的工作

虽然当前版本已建立“可运行基线”，但以下内容仍在开发中。

### 8.1 功能层

- homework 模块完整联调。
- chat 模块联调。
- parent 模块联调。
- 完整主链路回归测试。
- 接口错误码与异常处理规范补齐。

### 8.2 工程层

- 更系统的 README 补充。
- 调试说明完善。
- 提交规范落地。
- 标签 / 版本管理规范建立。
- 前后端联调脚本完善。

## 9. 后续开发建议

### 9.1 开发顺序建议

优先级建议如下：

1. auth 模块稳定性确认
2. pets 模块补齐联调
3. homework 模块接入联调页
4. chat 模块基础联调
5. parent 模块基础联调
6. 做一次完整主链路回归

### 9.2 主链路定义

当前建议始终围绕以下主链路推进：

```text
注册/登录 -> 创建宠物 -> 获取宠物 -> 提交作业 -> 奖励联动 -> 家长查看
```

## 10. 基线标签建议

建议在 `main` 分支上为本基线打 tag：

```text
v0.1.0-baseline
```

用于表示：

- 第一个本地可运行版本
- 已完成首次 GitHub 入库
- 已验证 auth / pets 核心链路

## 11. 如何使用这份基线

在以下场景中，应优先回看本文件：

- 新成员接手项目
- 本地环境出现异常
- 接口突然不可用
- 分支改动后需要排查回归问题
- 需要确认“2026-03-25 那个能跑的版本到底是什么样”

## 12. 结论

本文件对应的版本，是 `buddy-server` 从“环境不稳定、无法推送、无法联调”进入“本地可运行、已入 Git、可继续开发”的关键里程碑。

后续任何功能开发，都应以该基线为参考，避免在环境与基础设施问题上重复踩坑。
